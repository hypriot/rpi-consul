#debug: true

build:
  image: armdrone/drone-plugin-base
  commands:
    - apk update && apk add tar gzip zip curl
    - export VERSION=0.6.0
    - export UI_VERSION=${VERSION}
    - export URL=https://releases.hashicorp.com/consul/${VERSION}/consul_${VERSION}_linux_arm.zip
    - export UI_URL=https://releases.hashicorp.com/consul/${UI_VERSION}/consul_${UI_VERSION}_web_ui.zip
    - mkdir -p content/ui
    - curl -L ${URL} > ./consul.zip
    - curl -L ${UI_URL} > ./consul_ui.zip
    - unzip -d content/ consul.zip
    - unzip -d content/ui/ consul_ui.zip
    - ls -la content/

publish:
  docker:
#    environment:
#      - DOCKER_LAUNCH_DEBUG=true
    username: $$DOCKER_USER
    password: $$DOCKER_PASS
    email: $$DOCKER_EMAIL
    repo: hypriot/rpi-consul
    tag: 
      - latest
      - "0.6.0"

notify:
  slack:
    webhook_url: $$SLACK_WEBHOOK_URL
    channel: buildstatus
    username: Drone
    when:
      started: false
      success: true
      failure: true
